Ext.define('SCD.controller.scoreSubmitCtl', {
    extend: 'Ext.app.Controller',
	//views: [''],
	//this function runs after the launch function in app.js
	init: function() {
		//not yet developped..
	}
});